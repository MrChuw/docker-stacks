services:
    request-baskets:
        image: ghcr.io/mrchuw/rbaskets:latest
        container_name: request-baskets
        # ports:
        #     - "55555:55555"
        volumes:
            - ./baskets_data:/var/lib/rbaskets
        environment:
            - LISTEN=0.0.0.0
            - DB=bolt
            - FILE=/var/lib/rbaskets/baskets.db
            - THEME=adaptive
            - TOKEN=${TOKEN}
            - PATHPREFIX=${PATHPREFIX}
            - MODE=restricted
        restart: unless-stopped
        networks:
            - caddy

networks:
    caddy:
        external: true
