services:
  Alias_site:
    image: ghcr.io/mrchuw/alias-site:main
    container_name: Alias_site
    # ports:
    #   - ${PORT:-12836}:5454
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - caddy

networks:
  caddy:
    external: true